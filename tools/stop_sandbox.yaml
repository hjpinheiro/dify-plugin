identity:
  name: stop_sandbox
  author: daytonaio
  label:
    en_US: Stop Sandbox (deprecated)
    zh_Hans: Stop Sandbox (deprecated)
    ja_JP: Stop Sandbox (deprecated)
    pt_BR: Stop Sandbox (deprecated)
description:
  human:
    en_US: Stop a running Daytona sandbox to reduce cost without losing data.
    zh_Hans: Stop a running Daytona sandbox to reduce cost without losing data.
    ja_JP: Stop a running Daytona sandbox to reduce cost without losing data.
    pt_BR: Stop a running Daytona sandbox to reduce cost without losing data.
  llm: >
    DEPRECATED: use manage_sandbox(action=stop) instead. — Stop a running Daytona sandbox to pause execution and reduce cost. The sandbox state and all files are preserved. Use manage_sandbox(action=start) to resume it later.
parameters:
  - name: sandbox_id
    type: string
    required: false
    label:
      en_US: Sandbox ID
      zh_Hans: Sandbox ID
      ja_JP: Sandbox ID
      pt_BR: Sandbox ID
    human_description:
      en_US: The ID of the sandbox to stop. If omitted, the active sandbox is used.
      zh_Hans: The ID of the sandbox to stop. If omitted, the active sandbox is used.
      ja_JP: The ID of the sandbox to stop. If omitted, the active sandbox is used.
      pt_BR: The ID of the sandbox to stop. If omitted, the active sandbox is used.
    llm_description: 'The ID of the Daytona sandbox. If omitted, the active sandbox from this conversation is used.'
    form: llm
extra:
  python:
    source: tools/stop_sandbox.py
